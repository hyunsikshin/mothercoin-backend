type User {
  id: Int!
  pubkey: String!
  domain: String!
  email: String!
}

type Query {
  users: [User]!
  user(domain: String, pubkey: String, email: String, type: String!): User
}

type Mutation {
  addUser(pubkey: String!, domain: String!, email: String!): User!
}
