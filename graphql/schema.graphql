type User {
  id: Int!
  address: String!
  domain: String!
  email: String!
}

type Query {
  users: [User]!
  user(domain: String, address: String, email: String, type: String!): User
}

type Mutation {
  addUser(address: String!, domain: String!, email: String!): User!
}
